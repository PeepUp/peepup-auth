/* import { FastifyInstance, FastifyPluginAsync } from "fastify";
import fp from "fastify-plugin";
import type { Identity } from "@/types/main";
import certs from "../config/certs.config";

export const certsPlugin: FastifyPluginAsync = fp(async (fastify: FastifyInstance) => {
    fastify.decorate<Identity.Config.Certs>("certs", certs);
});
 */
